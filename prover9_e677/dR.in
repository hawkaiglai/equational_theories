assign(max_seconds,120). assign(max_weight,60).
formulas(assumptions).
  x * (x \ y) = y.
  x \ (x * y) = y.
  x = y * (x * ((y * x) * y)).
end_of_list.
formulas(goals).
  (a * c) * a = c & (b * c) * b = c -> a = b.
end_of_list.
